'use strict';

var ShowMore = require('../show-more');

describe('ShowMore View', function() {

  beforeEach(function() {
    this.showMore = new ShowMore();
  });

  it('Should run a few assertions', function() {
    expect(this.showMore);
  });

});
